import 'package:flutter/material.dart';

class SmoithieTab extends StatelessWidget {
  const SmoithieTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("SmoithieTab"));
  }
}